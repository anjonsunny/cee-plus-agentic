# Agentic additions, Stage 1 (perception). Install on top of root requirements:
#   pip install -r requirements.txt -r agentic/requirements.txt
# On Apple Silicon, plain `pip install torch` gives the MPS-enabled build.
pydantic>=2.0
torch>=2.2
transformers>=4.44
langgraph>=0.2      # dialogue agent (and the Stage 2 pipeline graph to come)
# Rulebook RAG (Stage 2): semantic retrieval over rule chunks.
# If absent, rulebook_rag.search() degrades to keyword scoring (logged).
llama-index-core>=0.11
llama-index-vector-stores-chroma>=0.2
llama-index-embeddings-huggingface>=0.3
chromadb>=0.5
